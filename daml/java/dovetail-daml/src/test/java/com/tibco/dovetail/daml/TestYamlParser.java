package com.tibco.dovetail.daml;

import static org.junit.Assert.*;

import org.apache.commons.lang3.builder.ReflectionToStringBuilder;
import org.apache.commons.lang3.builder.ToStringStyle;
import org.junit.Test;
public class TestYamlParser {
	
	@Test
	public void testYamlJackson(String file) {
//		ObjectMapper mapper = new ObjectMapper(new YAMLFactory());
        
		   //         AppConfig config = mapper.readValue(new File(args[0]), AppConfig.class);
		   //      System.out.println(ReflectionToStringBuilder.toString(config, ToStringStyle.MULTI_LINE_STYLE));
        
	}
}
